This directory contains the canlogserver from https://github.com/linux-can/can-utils. 
Changes are made to the Makefile (only build canlogserver) and canlogserver.c (introduce crash/bug).
Programs not related to the canlogserver are ommited.
To know which version of a file is used, the related commit is mentioned at the beginning of every file.